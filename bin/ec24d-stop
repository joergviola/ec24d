#!/bin/bash

# Usage: ec24d-stop

. ./ec24d

INSTANCES=`ec24d-list`

elb-deregister-instances-from-lb  $ELB --region $REGION --instances $INSTANCES
ec2-terminate-instances --region $REGION $INSTANCES
