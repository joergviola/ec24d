#!/bin/bash

# Usage: ec24d-list app-name
REGION=eu-west-1

OUTPUT=$(ec2-describe-instances --region $REGION -filter tag:app=$1 -filter instance-state-name=running)
echo "$OUTPUT" | awk '/^INSTANCE/ {print $2}'
